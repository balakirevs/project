<% provide(:title, "Редактировать") %> 
<div class="img-edit"> 
  <%= link_to image_tag("mojavesna.png", alt: "Весна"), root_path %>
</div>
<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Сохранить", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>