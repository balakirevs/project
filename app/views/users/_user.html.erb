<li>
  <h3><%= link_to user.name, user %></h3>
  <%= image_tag user.image_url(:thumb).to_s %><br />

  Возраст: <%= Time.now.year - user.birthdate.year %> года(лет)<br />

  Город: <%= user.city %><br />

  На сайте: <%= Time.now.month - user.created_at.month %> месяцев и <%= Time.now.day - user.created_at.day %> дней
</li> 
<section>
      <%= render 'shared/stats' %>
</section>
  </aside>
<div class="span8">
    <%= render 'follow_form' if signed_in? %>
</div> 